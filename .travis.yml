language: elixir
elixir:
    - 1.3.2
addons:
    postgresql: '9.5'
services:
    - postgresql
before_script:
    - psql -c 'create database travis_rosa_test;' -U postgres
    - cp config/travis.exs config/test.exs
    - mix do ecto.create, ecto.migrate
script:
    -mix test
